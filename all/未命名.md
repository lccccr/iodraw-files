```mermaid
graph TD
    A[fa:fa-user Tom] -->|Get money| B(Go shopping)
    B --> C{Select}
    C -->|One| D[Laptop]
    C -->|Two| E[iPhone]
    C -->|Three| F[fa:fa-car Car]
    A([开始]) --> B[使用YOLOv8检测I_t，获取动态目标边界框 B_i]
    B --> C[初始化静态点云 P_t^static = ∅]
    C --> D{遍历点云P_t中每个点P_j}
    D --> E[将P_j投影到图像平面，得到(u_j, v_j)]
    E --> F{是否存在B_i包含(u_j, v_j)?}
    F -->|是| G[标记为动态点，跳过]
    F -->|否| H[将P_j加入P_t^static]
    G --> I{是否处理完所有点?}
    H --> I
    I -->|否| D
    I -->|是| J[返回静态点云P_t^static]
    J --> K([结束])

```